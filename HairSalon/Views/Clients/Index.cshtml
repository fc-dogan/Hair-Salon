@{
  Layout = "_Layout";
}

@using HairSalon.Models;
@model List<HairSalon.Models.Client>;

<h1>Clients</h1>

@if (@Model.Count == 0)
{
  <h3>No clients have been added yet!</h3>
} 


@foreach (Client client in Model)
{
  <li>@client.Name
    <p>stylist: @Html.ActionLink($"{client.Stylist.Name}", "Details", "Stylists", new { id = client.Stylist.StylistId })</p>
  </li>
}

<p>@Html.ActionLink("Add new client", "Create")</p>
@* <p>@Html.ActionLink("Back to Stylist list", "Details", new { id = @Stylist.StylistId })</p> *@
<p>@Html.ActionLink("Home", "Index", "Home")</p>